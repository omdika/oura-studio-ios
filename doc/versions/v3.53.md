# Version 3.53 - Export JSON Laporan Penjualan (untuk Analisa AI)

## 1. Problem / Opportunity
*   **Context:** Laporan penjualan saat ini hanya bisa dilihat di layar app secara agregat (total revenue, profit per periode). Data mentah transaksi tidak bisa diekspor keluar app. Owner ingin menganalisa data penjualan menggunakan AI (ChatGPT/Claude) namun tidak ada cara untuk mengekstrak data secara terstruktur.
*   **Objective:** Menambahkan tombol "Export JSON" di halaman Laporan Penjualan yang menghasilkan file JSON berisi data transaksi selengkap-lengkapnya — mencakup semua detail order, item produk, harga, HPP, profit, customer, payment method, dan metadata periode — siap dipakai sebagai input analisa AI.

## 2. Approach: Client-Side JSON Composition (No New Backend Endpoint)

Semua data yang dibutuhkan sudah tersedia dari 3 endpoint existing:

| Source | Endpoint | Data |
|---|---|---|
| Raw orders + items | `GET /sales-orders?from=&to=` | Detail per transaksi (invoice, customer, payment, items+HPP+profit) |
| Period summary | `GET /reports/sales?from=&to=` | Revenue, profit, order count per hari |
| By-product ranking | `GET /reports/sales-by-product?from=&to=` | Qty sold, revenue, profit per product variant |

Frontend fetch ketiga endpoint → compose menjadi 1 JSON → export file via Share Sheet. **Zero backend changes required.**

## 3. User Flow & UX Specifications

### Entry Point
Tombol "Export JSON" ditambahkan di `SalesReportDetailView`, muncul di bawah section "Ringkasan" (setelah summary cards), hanya visible ketika data sudah loaded dan ada minimal 1 transaksi.

### UI Components
*   **Tombol Export:** `Button` dengan style OuraTheme accent, icon `square.and.arrow.up`, label "Export JSON untuk AI".
*   **Loading State:** Saat fetch + compose, tampilkan `ProgressView` inline di tombol, label berubah ke "Menyiapkan...".
*   **Share Sheet:** Setelah file JSON berhasil digenerate, tampilkan iOS standard share sheet (`UIActivityViewController`) agar user bisa share via AirDrop, iMessage, email, save to Files, dll.
*   **File Naming:** `oura_penjualan_{from}_{to}.json` (contoh: `oura_penjualan_2026-08-14_2026-09-13.json`).

### Validation Rules
*   Tombol hanya visible jika `report != nil && !report.points.isEmpty`.
*   Jika fetch gagal, tampilkan alert error.

## 4. Frontend Implementation

### 4a. SalesReportDetailView — Tambahan State & Button

File: `oura studio frontend/Screens/Lainnya/ReportsView.swift`

**State baru:**
```swift
@State private var isExporting = false
@State private var exportError: String?
@State private var exportFileURL: URL?
@State private var showShareSheet = false
```

**Button** (di dalam VStack utama, setelah Ringkasan card, sebelum "Per Periode"):
```swift
if report != nil && !(report?.points.isEmpty ?? true) {
    Button(action: { Task { await exportJSON() } }) {
        HStack(spacing: 8) {
            if isExporting {
                ProgressView().controlSize(.small).tint(.white)
            } else {
                Image(systemName: "square.and.arrow.up")
            }
            Text(isExporting ? "Menyiapkan..." : "Export JSON untuk AI")
                .font(.system(size: 14, weight: .semibold))
        }
        .frame(maxWidth: .infinity)
        .padding(.vertical, 12)
        .background(OuraTheme.Colors.accent)
        .foregroundStyle(.white)
        .clipShape(RoundedRectangle(cornerRadius: OuraTheme.Radius.medium))
    }
    .disabled(isExporting)
}
```

**Sheet & Alert:**
```swift
.sheet(isPresented: $showShareSheet) {
    if let url = exportFileURL {
        ActivityViewRepresentable(activityItems: [url])
    }
}
.alert("Gagal Export", isPresented: .constant(exportError != nil)) {
    Button("OK") { exportError = nil }
} message: {
    Text(exportError ?? "")
}
```

### 4b. Export Logic — `exportJSON()` Method

Compose JSON dari data yang sudah ada + fetch raw orders:

```swift
private func exportJSON() async {
    isExporting = true
    exportError = nil
    do {
        // 1. Fetch raw sales orders for the period
        let orders = try await api.getSalesOrders(from: fromDate, to: toDate)
        
        // 2. Compose the full export dictionary
        let export = composeExportJSON(
            report: report!,
            byProduct: byProduct,
            orders: orders,
            from: fromDate,
            to: toDate
        )
        
        // 3. Serialize to pretty-printed JSON
        let jsonData = try JSONSerialization.data(withJSONObject: export, options: [.prettyPrinted, .sortedKeys])
        
        // 4. Write to temp file
        let fmt = DateFormatter(); fmt.dateFormat = "yyyy-MM-dd"
        let fileName = "oura_penjualan_\(fmt.string(from: fromDate))_\(fmt.string(from: toDate)).json"
        let tempURL = FileManager.default.temporaryDirectory.appendingPathComponent(fileName)
        try jsonData.write(to: tempURL)
        
        exportFileURL = tempURL
        showShareSheet = true
    } catch {
        exportError = error.localizedDescription
    }
    isExporting = false
}
```

### 4c. JSON Composition — `composeExportJSON()` Helper

```swift
private func composeExportJSON(
    report: SalesReport,
    byProduct: [SalesByProductItem],
    orders: [SalesOrder],
    from: Date, to: Date
) -> [String: Any] {
    let fmt = DateFormatter(); fmt.dateFormat = "yyyy-MM-dd"
    let isoFmt = ISO8601DateFormatter()
    
    let nonCancelled = orders.filter { !$0.isCancelled }
    let totalDiscount = nonCancelled.flatMap(\.items).reduce(0.0) { $0 + $1.discount * Double($1.qty) }
    
    // Revenue by payment method
    var revByPM: [String: Double] = [:]
    var countByPM: [String: Int] = [:]
    var countByStatus: [String: Int] = [:]
    for order in orders {
        countByStatus[order.status, default: 0] += 1
        guard !order.isCancelled else { continue }
        let pm = order.paymentMethod ?? "unknown"
        revByPM[pm, default: 0] += order.displayRevenue
        countByPM[pm, default: 0] += 1
    }
    
    let totalCogs = nonCancelled.flatMap(\.items).reduce(0.0) { $0 + $1.unitHppSnapshot * Double($1.qty) }
    let avgMargin = report.totalRevenue > 0 ? ((report.totalRevenue - totalCogs) / report.totalRevenue) * 100 : 0
    
    return [
        "export_info": [
            "generated_at": isoFmt.string(from: Date()),
            "period_from": fmt.string(from: from),
            "period_to": fmt.string(from: to),
            "total_orders": orders.count,
            "total_items_sold": nonCancelled.flatMap(\.items).reduce(0) { $0 + $1.qty },
            "currency": "IDR"
        ],
        "summary": [
            "total_revenue": report.totalRevenue,
            "total_cogs": totalCogs,
            "total_profit": report.totalProfit,
            "avg_profit_margin_pct": round(avgMargin * 100) / 100,
            "total_discount_given": totalDiscount,
            "revenue_by_payment_method": revByPM,
            "order_count_by_payment_method": countByPM,
            "order_count_by_status": countByStatus,
            "top_selling_products": byProduct.prefix(10).map { item in
                [
                    "product_name": item.productName,
                    "size_label": item.sizeLabel,
                    "fabric_variant_name": item.fabricVariantName as Any,
                    "qty_sold": item.qtySold,
                    "revenue": item.revenue
                ] as [String: Any]
            },
            "daily_trend": report.points.map { pt in
                [
                    "date": pt.period,
                    "order_count": pt.orderCount,
                    "units_sold": pt.unitsSold ?? 0,
                    "revenue": pt.totalRevenue,
                    "profit": pt.totalProfit
                ] as [String: Any]
            }
        ] as [String: Any],
        "orders": orders.map { order in
            let orderCogs = order.items.reduce(0.0) { $0 + $1.unitHppSnapshot * Double($1.qty) }
            let orderDiscount = order.items.reduce(0.0) { $0 + $1.discount * Double($1.qty) }
            return [
                "order_id": order.id.uuidString,
                "invoice_no": order.invoiceNo,
                "sold_at": isoFmt.string(from: order.soldAt),
                "status": order.status,
                "customer_name": order.customerName as Any,
                "payment_method": order.paymentMethod as Any,
                "marketplace_fee_pct": order.marketplaceFeePct,
                "order_total_revenue": order.isCancelled ? 0 : order.displayRevenue,
                "order_total_cogs": order.isCancelled ? 0 : orderCogs,
                "order_total_profit": order.isCancelled ? 0 : order.displayProfit,
                "order_total_discount": orderDiscount,
                "items": order.items.map { item in
                    let eff = item.effectivePrice
                    let lineRev = item.lineRevenue
                    let lineCogs = item.unitHppSnapshot * Double(item.qty)
                    let marginPct = lineRev > 0 ? (item.lineProfit / lineRev) * 100 : 0
                    return [
                        "item_id": item.id.uuidString,
                        "product_name": item.productName as Any,
                        "size_label": item.sizeLabel as Any,
                        "qty": item.qty,
                        "unit_price": item.unitPrice,
                        "discount": item.discount,
                        "effective_price": eff,
                        "line_revenue": lineRev,
                        "unit_hpp_snapshot": item.unitHppSnapshot,
                        "line_cogs": lineCogs,
                        "line_profit": item.lineProfit,
                        "profit_margin_pct": round(marginPct * 100) / 100
                    ] as [String: Any]
                }
            ] as [String: Any]
        }
    ]
}
```

### 4d. ShareSheet Wrapper (jika belum ada)

File baru: `oura studio frontend/Components/ActivityViewRepresentable.swift`

```swift
import SwiftUI
import UIKit

struct ActivityViewRepresentable: UIViewControllerRepresentable {
    let activityItems: [Any]
    
    func makeUIViewController(context: Context) -> UIActivityViewController {
        UIActivityViewController(activityItems: activityItems, applicationActivities: nil)
    }
    
    func updateUIViewController(_ vc: UIActivityViewController, context: Context) {}
}
```

## 5. Contoh Lengkap Format JSON Output

Berikut file JSON yang akan dihasilkan — mendemonstrasikan semua field & edge case:

```json
{
  "export_info": {
    "generated_at": "2026-09-13T14:30:00Z",
    "period_from": "2026-08-14",
    "period_to": "2026-09-13",
    "total_orders": 5,
    "total_items_sold": 14,
    "currency": "IDR"
  },
  "summary": {
    "total_revenue": 1320000,
    "total_cogs": 396000,
    "total_profit": 924000,
    "avg_profit_margin_pct": 70.0,
    "total_discount_given": 45000,
    "revenue_by_payment_method": {
      "cash": 420000,
      "transfer": 450000,
      "qris": 300000,
      "marketplace": 150000
    },
    "order_count_by_payment_method": {
      "cash": 2,
      "transfer": 1,
      "qris": 1,
      "marketplace": 1
    },
    "order_count_by_status": {
      "paid": 4,
      "unpaid": 0,
      "cancelled": 1
    },
    "top_selling_products": [
      {
        "product_name": "Scrunchie",
        "size_label": "M",
        "fabric_variant_name": "Satin Pelangi",
        "qty_sold": 5,
        "revenue": 500000
      },
      {
        "product_name": "Scrunchie",
        "size_label": "L",
        "fabric_variant_name": "Waffle Merah",
        "qty_sold": 4,
        "revenue": 360000
      },
      {
        "product_name": "Scrunchie",
        "size_label": "S",
        "fabric_variant_name": null,
        "qty_sold": 3,
        "revenue": 270000
      },
      {
        "product_name": "Claw Clip Bow",
        "size_label": "M",
        "fabric_variant_name": "Organza Pink",
        "qty_sold": 2,
        "revenue": 190000
      }
    ],
    "daily_trend": [
      {
        "date": "2026-08-20",
        "order_count": 1,
        "units_sold": 3,
        "revenue": 270000,
        "profit": 189000
      },
      {
        "date": "2026-08-25",
        "order_count": 1,
        "units_sold": 2,
        "revenue": 190000,
        "profit": 130000
      },
      {
        "date": "2026-09-01",
        "order_count": 1,
        "units_sold": 4,
        "revenue": 360000,
        "profit": 252000
      },
      {
        "date": "2026-09-10",
        "order_count": 1,
        "units_sold": 3,
        "revenue": 350000,
        "profit": 248000
      },
      {
        "date": "2026-09-13",
        "order_count": 1,
        "units_sold": 2,
        "revenue": 150000,
        "profit": 105000
      }
    ]
  },
  "orders": [
    {
      "order_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
      "invoice_no": "INV-20260820-001",
      "sold_at": "2026-08-20T09:30:00Z",
      "status": "paid",
      "customer_name": "Rina Putri",
      "payment_method": "cash",
      "marketplace_fee_pct": 0,
      "order_total_revenue": 270000,
      "order_total_cogs": 81000,
      "order_total_profit": 189000,
      "order_total_discount": 0,
      "items": [
        {
          "item_id": "11111111-2222-3333-4444-555555555555",
          "product_name": "Scrunchie",
          "size_label": "S",
          "qty": 3,
          "unit_price": 90000,
          "discount": 0,
          "effective_price": 90000,
          "line_revenue": 270000,
          "unit_hpp_snapshot": 27000,
          "line_cogs": 81000,
          "line_profit": 189000,
          "profit_margin_pct": 70.0
        }
      ]
    },
    {
      "order_id": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
      "invoice_no": "INV-20260825-001",
      "sold_at": "2026-08-25T14:15:00Z",
      "status": "paid",
      "customer_name": null,
      "payment_method": "qris",
      "marketplace_fee_pct": 0,
      "order_total_revenue": 190000,
      "order_total_cogs": 60000,
      "order_total_profit": 130000,
      "order_total_discount": 0,
      "items": [
        {
          "item_id": "22222222-3333-4444-5555-666666666666",
          "product_name": "Claw Clip Bow",
          "size_label": "M",
          "qty": 2,
          "unit_price": 95000,
          "discount": 0,
          "effective_price": 95000,
          "line_revenue": 190000,
          "unit_hpp_snapshot": 30000,
          "line_cogs": 60000,
          "line_profit": 130000,
          "profit_margin_pct": 68.42
        }
      ]
    },
    {
      "order_id": "c3d4e5f6-a7b8-9012-cdef-123456789012",
      "invoice_no": "INV-20260901-001",
      "sold_at": "2026-09-01T11:00:00Z",
      "status": "paid",
      "customer_name": "Dian",
      "payment_method": "transfer",
      "marketplace_fee_pct": 0,
      "order_total_revenue": 360000,
      "order_total_cogs": 108000,
      "order_total_profit": 252000,
      "order_total_discount": 20000,
      "items": [
        {
          "item_id": "33333333-4444-5555-6666-777777777777",
          "product_name": "Scrunchie",
          "size_label": "L",
          "qty": 4,
          "unit_price": 95000,
          "discount": 5000,
          "effective_price": 90000,
          "line_revenue": 360000,
          "unit_hpp_snapshot": 27000,
          "line_cogs": 108000,
          "line_profit": 252000,
          "profit_margin_pct": 70.0
        }
      ]
    },
    {
      "order_id": "d4e5f6a7-b8c9-0123-defa-234567890123",
      "invoice_no": "INV-20260910-001",
      "sold_at": "2026-09-10T16:45:00Z",
      "status": "paid",
      "customer_name": "Shopee - anindhita_acc",
      "payment_method": "marketplace",
      "marketplace_fee_pct": 6.5,
      "order_total_revenue": 350000,
      "order_total_cogs": 102000,
      "order_total_profit": 248000,
      "order_total_discount": 25000,
      "items": [
        {
          "item_id": "44444444-5555-6666-7777-888888888888",
          "product_name": "Scrunchie",
          "size_label": "M",
          "qty": 2,
          "unit_price": 100000,
          "discount": 0,
          "effective_price": 100000,
          "line_revenue": 200000,
          "unit_hpp_snapshot": 17000,
          "line_cogs": 34000,
          "line_profit": 166000,
          "profit_margin_pct": 83.0
        },
        {
          "item_id": "55555555-6666-7777-8888-999999999999",
          "product_name": "Scrunchie",
          "size_label": "M",
          "qty": 1,
          "unit_price": 175000,
          "discount": 25000,
          "effective_price": 150000,
          "line_revenue": 150000,
          "unit_hpp_snapshot": 68000,
          "line_cogs": 68000,
          "line_profit": 82000,
          "profit_margin_pct": 54.67
        }
      ]
    },
    {
      "order_id": "e5f6a7b8-c9d0-1234-efab-345678901234",
      "invoice_no": "INV-20260913-001",
      "sold_at": "2026-09-13T10:15:30Z",
      "status": "cancelled",
      "customer_name": "Budi",
      "payment_method": "cash",
      "marketplace_fee_pct": 0,
      "order_total_revenue": 0,
      "order_total_cogs": 0,
      "order_total_profit": 0,
      "order_total_discount": 0,
      "items": [
        {
          "item_id": "66666666-7777-8888-9999-aaaaaaaaaaaa",
          "product_name": "Scrunchie",
          "size_label": "M",
          "qty": 2,
          "unit_price": 100000,
          "discount": 0,
          "effective_price": 100000,
          "line_revenue": 200000,
          "unit_hpp_snapshot": 17000,
          "line_cogs": 34000,
          "line_profit": 166000,
          "profit_margin_pct": 83.0
        }
      ]
    }
  ]
}
```

### Catatan Edge Cases dalam Contoh:
1. **Order cancelled** (INV-20260913-001) — tetap ada di `orders[]` tapi `order_total_*` di-nol-kan. AI bisa lihat pattern pembatalan.
2. **`customer_name: null`** (INV-20260825-001) — walk-in tanpa nama. AI bisa hitung % transaksi tanpa data customer.
3. **`marketplace_fee_pct: 6.5`** (INV-20260910-001) — transaksi marketplace. AI bisa hitung net profit setelah fee.
4. **Multiple items dalam 1 order** (INV-20260910-001) — cross-sell yang bisa dianalisa AI.
5. **Diskon per item** (INV-20260901-001, INV-20260910-001) — AI bisa analisa efektivitas diskon terhadap volume.

## 6. Verification & Test Plan

### Smoke Test
1. Build sukses tanpa error.
2. Buka Lainnya → Laporan Penjualan → tombol "Export JSON untuk AI" muncul setelah data loaded.

### Sanity Test Scenarios
1. **Happy path:** Set rentang tanggal → tap Export → share sheet muncul → file JSON valid.
2. **Empty state:** Rentang tanpa transaksi → tombol export hidden.
3. **File content:** Buka file JSON, verifikasi semua field lengkap, angka konsisten.
4. **Cancelled order:** Order cancelled ada di JSON dengan `order_total_revenue: 0`.
5. **Share destinations:** File bisa di-save to Files, AirDrop, copy.
