# v3.25 — Filter Rentang Tanggal di Halaman Penjualan

**Status:** DONE
**Date:** 2026-08-25
**Changed by:** Design + Frontend + Backend
**Backend changes required:** Ya, menambahkan parameter `from` dan `to` di endpoint `GET /sales-orders` pada router `sales.py` di backend.

---

## Latar Belakang

Owner ingin menyaring daftar transaksi penjualan secara dinamis berdasarkan rentang tanggal tertentu langsung dari tab **Penjualan** (`PenjualanListView.swift`), sama halnya dengan halaman laporan, agar pencarian transaksi lama berjalan rapi dan terarah.

v3.25 mengimplementasikan penyaring rentang tanggal (`DateRangeField`) di bagian atas daftar penjualan, sekaligus menyempurnakan struktur kode dengan mengekstrak `DateRangeField` menjadi komponen bersama (*shared component*) yang modular.

---

## A. Desain UI & Penataan Ulang Komponen (Frontend)

### 1. Ekstraksi Shared Component (`DateRangeField.swift`)
- Memindahkan definisi `DateRangeField` dan `DateRangePickerSheet` dari file privat `ReportsView.swift` ke file komponen publik baru di `Components/DateRangeField.swift`.
- Mengganti seluruh referensi `DateRangeField` di `ReportsView.swift` agar mengonsumsi komponen bersama ini tanpa mengubah behavior visualnya.

### 2. Integrasi ke Halaman Penjualan (`PenjualanListView.swift`)
- Menambahkan binding `@State private var fromDate` (default 30 hari yang lalu) dan `toDate` (default hari ini) pada `PenjualanListView`.
- Menyisipkan `DateRangeField` dibungkus dalam layout VStack yang diletakkan di bagian paling atas layar, di atas list penjualan.
- Jika data kosong pada periode yang dipilih, aplikasi menampilkan ilustrasi dan pesan informatif yang ramah: `"Tidak ada transaksi dalam periode ini, ketuk + untuk mencatat penjualan baru"`.

---

## B. Spesifikasi API & Perubahan Backend (`../backend`)

### 1. Penambahan Parameter Router (`app/routers/sales.py`)
Endpoint `GET /sales-orders` diperbarui untuk menerima parameter query tanggal:

```python
@router.get("", response_model=list[SalesOrderOut])
def list_sales_orders(
    status_filter: str | None = Query(default=None, alias="status"),
    from_: date | None = Query(default=None, alias="from"),
    to: date | None = Query(default=None),
    limit: int = Query(default=50, le=200),
    offset: int = 0,
    db: Session = Depends(get_db),
):
    q = db.query(SalesOrder).options(joinedload(SalesOrder.items))
    if status_filter is not None:
        q = q.filter(SalesOrder.status == status_filter)
    if from_ is not None:
        start = datetime.combine(from_, datetime.min.time(), tzinfo=timezone.utc)
        q = q.filter(SalesOrder.sold_at >= start)
    if to is not None:
        end = datetime.combine(to, datetime.min.time(), tzinfo=timezone.utc) + timedelta(days=1)
        q = q.filter(SalesOrder.sold_at < end)
        
    orders = q.order_by(SalesOrder.sold_at.desc()).offset(offset).limit(limit).all()
    return _orders_out(db, orders)
```

---

## C. File yang Berubah

### Frontend (`/Users/handika/Desktop/PROJECT/oura-studio/frontend/oura studio frontend`)
| File | Perubahan |
|---|---|
| `Components/DateRangeField.swift` | [NEW] Berkas komponen bersama untuk `DateRangeField` dan `DateRangePickerSheet`. |
| `Screens/Lainnya/ReportsView.swift` | Hapus definisi privat `DateRangeField` dan `DateRangePickerSheet` agar menggunakan komponen bersama yang baru. |
| `Screens/Penjualan/PenjualanListView.swift` | Tambahkan filter `DateRangeField` di layout utama dan sinkronkan panggilan load API dengan parameter tanggal. |
| `Networking/APIService.swift` | Sinkronkan metode `getSalesOrders(from:to:)` agar mengirim parameter query string `from` dan `to` ke backend. |

### Backend (`/Users/handika/Desktop/PROJECT/oura-studio/backend`)
| File | Perubahan |
|---|---|
| `app/routers/sales.py` | Tambah parameter query `from` dan `to` pada `list_sales_orders` serta filter query SQL di database. |
